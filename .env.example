# MarDoc requires no environment variables for demo mode or GitHub PAT auth.
# Run npm ci, then npm run dev. Enter a PAT in Settings → GitHub Connection.
# See docs/setup-github-pat.md and docs/development.md.
# Do not put tokens in NEXT_PUBLIC_* variables: those values ship to browsers.
# Auth0 is not integrated into this application.
